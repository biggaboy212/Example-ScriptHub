{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with Darklua",
            "type": "shell",
            "command": "${workspaceFolder}\\build\\build.bat",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}